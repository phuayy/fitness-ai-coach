# MediaPipe Model Folder

Place your downloaded MediaPipe Pose Landmarker model here.

Expected default path:

```text
pose_landmarker_full.task
```

Accepted names can be configured in `app/config.py`.

The actual `.task` file is not included because it is a large external model artifact.
